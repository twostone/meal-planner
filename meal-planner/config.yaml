# https://developers.home-assistant.io/docs/apps/configuration
name: Essensplanung
# Bump on every change, otherwise Home Assistant shows no update.
version: "0.1.0"
slug: essensplanung
description: Wöchentliche Essensplanung für den Haushalt
url: "https://github.com/twostone/meal-planner"
arch:
  - aarch64
  - amd64
# No "image": Home Assistant builds the Dockerfile on the device.
ingress: true
ingress_port: 8099
panel_icon: mdi:silverware-fork-knife
panel_title: Essensplanung
# Default is admin-only; the whole household should see it.
panel_admin: false
